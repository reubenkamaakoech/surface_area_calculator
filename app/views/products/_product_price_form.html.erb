<div class="nested-form-wrapper" data-new-record="<%= f.object.new_record? %>"><br/>
  <table>
    <tbody>
      <tr>
        <td><%= f.label :amount %></td>
        <td><%= f.text_field :amount, class:"form-control" %></td>
        <td><button type="button" data-action="nested-form#remove" class="btn btn-outline-danger btn-sm">Remove</button></td>
      </tr>
   </tbody>
  </table>

    <%= f.hidden_field :_destroy %>
</div>